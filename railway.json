{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python tg_super_agent.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "variables": {
    "SESSION_STRING": {
      "description": "Telegram сессия (из get_session.py)",
      "required": true
    },
    "API_ID": {
      "description": "API ID из my.telegram.org (число)",
      "required": true
    },
    "API_HASH": {
      "description": "API Hash из my.telegram.org",
      "required": true
    },
    "GROQ_API_KEY": {
      "description": "Ключ Groq Cloud API (gsk_...)",
      "required": true
    }
  }
}
